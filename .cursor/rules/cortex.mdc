---
description: Persistent project context for Mozentai — consult before acting
alwaysApply: true
---

# BRAIN — Mozentai Site Context

## What This Project Is

Static **Jekyll 4.3** landing page for Mozentai, deployed to **GitHub Pages** at **mozentai.com** via `.github/workflows/pages.yml` on push to `main`.

**Tagline:** Engineers @ Work

## Repo layout

| Path | Purpose |
|------|---------|
| `_layouts/default.html` | Landing page: hero with nav, pitch, clients marquee, CTA, footer |
| `_layouts/inner.html` | Inner pages (contact form). Compact top bar with nav, no full-viewport hero |
| `_layouts/about.html` | About page: pitch, stats, services, certifications |
| `_layouts/professionals.html` | Professionals page: Career Wall lookup, title ladder |
| `_layouts/inbox.html` | `/login/` manager inbox. Looks like the 404 page until Victor is signed in from an allowed IP |
| `_includes/hire-cta.html` | Homepage closer: Get in touch |
| `_includes/pitch.html` | Below-fold hook: "Need engineers who actually ship?" |
| `_includes/stats.html` | 10 years in market, 15+ years experience |
| `_includes/career.html` | Career Wall lookup, registered engineers, title ladder |
| `_data/career.yml` | Career Wall copy and title ladder (Python Engineer I/II/III, exams P1/P2/P3) |
| `_data/exams.yml` | Per-title question groups. Empty until authored. Never render on the public page. |
| `_data/engineers.yml` | Registered CITC professionals. Empty until someone holds a CREA. Do not invent people. |
| `_includes/engagement.html` | Unused on the public homepage as of 2026-09-19. Hire form still lists the engagement options. |
| `_includes/clients.html` | Axonius-style client logo marquee |
| `_includes/certifications.html` | ISO/SOC/PCI badge grid |
| `_includes/testimonials.html` | Unused on the public site as of 2026-09-19. Invented quotes. Keep off the live page. |
| `_data/pitch.yml` | Audit pitch question and body copy |
| `_data/services.yml` | Auditing, Hardening, Engineering, CITC |
| `_data/engagement.yml` | Engagement models (Staff Augmentation, Dedicated Team, Project-Based) |
| `_data/stats.yml` | Years in market and specialist tenure (real figures from Victor, 2026-08-25) |
| `_data/leadership.yml` | Unused on the public site as of 2026-09-19. Victor asked to be removed from the Leadership section. Keep off the live page. |
| `_data/clients.yml` | `europe`, `united_states`, and `brazil` client lists |
| `_data/certifications.yml` | Market certifications |
| `assets/clients/*` | Client wordmark logos, PNG only (fictional brands plus SANEARES). Never commit or serve SVG client logos. |
| `assets/team/victor-matos.jpg` | CEO photo on disk, not displayed on the public site as of 2026-09-19. |
| `assets/glider.gif` | Animated Conway glider (hacker emblem), recolored to mint on `--bg` from Victor's `~/github/glider.gif` |
| `assets/glider.png` | Static first frame of the glider for `prefers-reduced-motion` |
| `about/index.md` | About page at `/about/` |
| `professionals/index.md` | Professionals page at `/professionals/` |
| `_data/hire.yml` | Contact page copy, form labels, destination email victor@mozentai.com |
| `hire/index.md` | Contact page at `/hire/` |
| `hire/sent.md` | Post-submit thank you at `/hire/sent/` |
| `login/index.md` | Manager inbox at `/login/`. Not linked from the public site. |
| `_layouts/account.html` | Engineer account page at `/account/`. Sign in with Google, $20/month CREA subscription via Stripe Payment Link, certifications dashboard with freshness. |
| `account/index.md` | Engineer account page at `/account/` |
| `assets/js/account.js` | Engineer account JS: Firebase Auth, Firestore `engineers/{uid}`, Stripe subscribe redirect |
| `_layouts/company.html` | Company account page at `/company/`. Sign in with Google, $1,000/month plan via Stripe Payment Link, engineer lookup. |
| `company/index.md` | Company account page at `/company/` |
| `assets/js/company.js` | Company account JS: Firebase Auth, Firestore `companies/{uid}`, Stripe subscribe redirect, engineer search |
| `_data/stripe.yml` | Stripe Payment Link URLs and Customer Portal URL. Not a secret (publishable links). Replace REPLACE_ME with real Stripe URLs. |
| `firestore.rules` | Firestore rules for inquiries, engineers, companies, and IP allowlist. Deploy with Firebase CLI. |
| `_data/firebase.yml` | Public Firebase web config for the contact form and inbox. Not a secret. |
| `404.md` | Custom 404 |
| `_config.yml` | Site config, `lang: en-US`, `jekyll-seo-tag` |

## Design system

```css
--bg: #0d1117;
--surface: #161b22;
--border: #21262d;
--accent: #00e5a0;
--muted: #5a6b82;
--text: #c9d1d9;
--mono: JetBrains Mono;
--sans: Inter;
```

- Dark cyber aesthetic, mint green accent
- Hero: `100dvh`, centered wordmark, "Explore" scroll hint with chevron
- Scroll reveal via `IntersectionObserver` on `.reveal` elements
- `prefers-reduced-motion` respected

## Page sections (top to bottom)

### Landing page (`/`)

1. **Hero** — MOZENT△I wordmark, tagline, nav links (About, Professionals, Get in touch). Mark sized to viewport lockup (up to 560px). Do not shrink to the tagline.
2. **Pitch** — "Stop wasting time on screening."
3. **Clients** — infinite horizontal marquee (EU, US, Brazil). Keep this section.
4. **Get in touch CTA** — button to `/hire/`.
5. **Footer** — Conway glider only.

### About page (`/about/`)

Topbar nav (About active, Professionals, Get in touch). Problem-first copy, professional tone. Headline: "Technical interviews are broken." Do not use slang ("crap"), quality guarantees, or punchy contrast lines. Then:
- Pitch + stats (10 years, 15+)
- Services (Auditing, Hardening, Engineering, CITC). 2x2 grid.
- Certifications (ISO 27001/27017/27018/9001/27701, SOC 2, PCI DSS, NIST CSF)

### Professionals page (`/professionals/`)

Topbar nav (About, Professionals active, Get in touch). Contains:
- Career Wall lookup by name, CITC number, title, or exam (P1, P2, P3). Registered engineers appear here. Title ladder: Python Engineer I, II, III. Cards show exam names. Public copy: questionnaire or schedule with an instructor. Portraits use site colors only. Do not print $20, $200, the five-title cap, PX, HTB, score bands, or question banks.

Do **not** put a Leadership / CEO photo section on the public site. Victor asked to be removed on 2026-09-19.
Do **not** put Testimonials or the How We Work engagement cards on the public site. Victor asked to remove the generic stuff on 2026-09-19 and keep the client companies.

**Contact page (`/hire/`):** Headline is "Get in touch". Form (name, work email, company, engagement model including CITC Credential, message) POSTs via FormSubmit to **victor@mozentai.com** and also writes a document to Firestore `inquiries`. Mailto fallback on the same page. Do not put this form in the hero. Do not name Victor on the public contact copy.

**Manager inbox (`/login/`):** Added 2026-09-19. Unlisted. Visitors see the same 404 copy ("Got lost on the way home?"). Click the wordmark, sign in with Google as spacemany2k38@gmail.com or victor@mozentai.com (verified email). After sign-in, the page checks the visitor IP against Firestore `config/allow.ips`. If the IP is not listed, it stays a 404. If it matches, it lists contact-form requests. Do not link `/login/` from the public footer or nav. Allowed IP is stored in Firestore, never in the public git repo. If Victor's IP changes, update `config/allow.ips`. Cloud Functions (true edge IP lock before any page JS) need the Firebase Blaze plan, which was not enabled.

Firebase project: `mozentai-com`. Inquiries are PII, they live in Firestore, never in this repo.

## Company facts (from Victor, 2026-08-25)

- Mozentai is an engineering company. Public copy is toward software engineers, professional in tone. Lead public pitch (2026-09-19): "Stop wasting time on screening." We prepare the professionals for the companies so they do not waste time on screening. Look them up on the Career Wall. Do not put fake testimonials or the generic How We Work cards on the live page. Keep the Clients marquee. Tagline: Engineers @ Work.
- **CITC** (Council of Information Technology & Computing): professional standing for software engineers, CREA/CRM analog. Public copy stays English. Do not print CREA/CRM, CPF, $20, $200, or "five titles" on the site. Mechanics below are real and must be followed in product work.

### CITC mechanics (Victor, 2026-09-19, not for the public page)

- **Identifier:** Each registered engineer (and each company) holds a SmartHex-16 number, the CITC CREA analog of CPF/CREA. Package: PyPI `smarthex` 0.2.2 by Victor Matos (contact@byteram.co), repo Byteram/SmartHex16. 16 hex chars: 14 random + 2 checksum. Generate/validate via `from smarthex import generate, validate`. Local checkout: `~/github/pypi-env/smarthex`. Example valid IDs generated 2026-09-19: `3f3ed5d05b617d16`, `529532bed881a1c9`, `331023a6abff6ed1`. Display on the wall as "CITC number", not "SmartHex".
- **Stripe is the source of standing.** CREA keep-alive and company plans are Stripe Billing. Title exams are Stripe one-time Checkout. Verify every webhook with the `Stripe-Signature` header. A CREA is live only while the keep-alive subscription is active. A title is granted only after the exam payment (and Mozentai's own pass/fail). Companies are likewise Stripe customers. Do not treat a SmartHex as valid without a matching signed Stripe event.
- **Engineer keep-alive:** USD 20 per month to keep the CREA. Stripe Checkout `mode: 'subscription'`. One Product named CITC CREA with one recurring Price $20/month. Do not put other tiers on that Product. Never pass `payment_method_types`. Latest Stripe API `2026-07-29.dahlia`. Restricted key (`rk_`) not `sk_`. Customer Portal for cancel/update. If charging US or EU, Stripe Tax needs an active registration before `automatic_tax`.
- **Title exam:** USD 200 to take the test for a given title. Stripe Checkout `mode: 'payment'`, one-time. One Product per exam code (P1, P2, P3, later PX and other titles) each with a $200 Price so Checkout line items show the exam name. Cap: **5 titles per CREA**. An engineer cannot hold everything under the sun.
- **Exam names (Victor, 2026-09-19):** Python titles use P1, P2, P3. Any code ending in **X** is specialist (PX for Python specialist). Do not invent a specialist title on the public catalog until Victor names it. Public wall shows P1, P2, P3 on the three Python Engineer cards.
- **Question groups:** Each level/title has its own question bank. Author them in `_data/exams.yml` under that exam code. Do not invent questions. Do not render the bank on mozentai.com.
- **Performance type:** After a sitting, generate a performance type from the candidate's test score. Do not invent score bands or type names until Victor supplies them. Same score-to-type step for both exam modes.
- **Two exam modes:** (1) Regular questionnaire. (2) Live with a tester: the candidate schedules the sitting and takes it with an instructor. Internal analogy is Hack The Box style practical work, but CITC is scheduled with an instructor, not a self-serve HTB box. Never print "Hack The Box", HTB, score bands, or the question bank on the public site. Public copy may say questionnaire or schedule a session with an instructor.
- **Public title catalog on 2026-09-19:** Python (P1/P2/P3/PX), Cybersecurity (C1/C2/C3/CX), Java (J1/J2/J3/JX), Go (G1/G2/G3/GX), Mathematics (M1/M2/M3/MX), Logic (L1/L2/L3/LX), Computer Science (CS1/CS2/CS3/CSX), Software Engineering (SE1/SE2/SE3/SSX), AWS (AW1/AW2/AW3/AWX), GCP (GC1/GC2/GC3/GCX), Azure (AZ1/AZ2/AZ3/AZX), Heroku (HK1/HK2/HK3/HKX), Firebase (FB1/FB2/FB3/FBX), Git (GT1/GT2/GT3/GTX), GitHub (GH1/GH2/GH3/GHX), GitLab (GL1/GL2/GL3/GLX). Requirements (exigencias) come only from Mozentai. Engineers look those titles up as what they want to become. They do not get a menu of outside credentials.
- **Career Wall:** Once registered, the engineer appears on the wall and can be looked up (name, CITC number, title, or exam). Do not invent people. Do not put Victor on the wall. Photos in `assets/engineers/`. Portraits must use site colors only (`--bg`, `--surface`, `--accent`, `--muted`, `--text`). No photographic color. CSS grayscale + mint color wash.
- **Company access:** Companies do not get the whole roster. Stripe plan determines which engineer group they can access. Public wall is lookup/display. Hiring access is plan-gated.
- **Engineer access:** Engineers see their own CREA and Mozentai titles/exigencias. They look up what they want to be. They do not browse every other engineer.
- **Public site:** Know the mechanics. Do not print prices, the five-title cap, Stripe, SmartHex, CPF, CREA, CRM, PX, Hack The Box, score bands, or exam questions on mozentai.com.
- Mozentai has been in the market for 10 years.
- Specialists have 15+ years of hands-on security experience. Do not publish a 10+ band on the site; Victor corrected this to 15+ only on 2026-08-25.
- **CEO:** Victor Matos (professional name; full name Victor Vinicius Reis de Matos). Photo: `assets/team/victor-matos.jpg`.
- **Hack The Box:** public profile [spacemany2k38](https://profile.hackthebox.com/profile/019cab82-c302-71b6-a445-abbfbbb76b89) (UUID `019cab82-c302-71b6-a445-abbfbbb76b89`). Handle `@spacemany2k38`. HTB is **not** linked from the public site as of 2026-09-19 (Leadership section removed). If it returns, show credential as abbreviated "HTB CJCA" only, NEVER expand to "Junior" on the site (Victor is an expert, not junior-level). Do not paste the HTB About text (Avenue Code, Axonius, etc.) onto mozentai.com. Link out. Strip `utm_*` from the URL. Never mention Bitly on this public site.
- Do not invent extra stats (audit counts, retention %, countries served) unless Victor supplies them. A previous INOTVMOS-inspired stats set (2,400+ fixes, 98% retention, 120+ audits, 14 countries) was marketing filler and was replaced.

## Clients (marquee)

- Full-width `marquee-wrap` breaks out of `.below` container
- Duplicated track for seamless `-50%` translate loop
- Logos: grayscale/muted by default, full color on hover
- Pause animation on hover

## Fictional brands (do not present as real companies)

**Clients Europe:** Rheinwerk AG, NordSec GmbH, Helvetic Data AG, Praxis Digital B.V.

**Clients US:** Meridian Health Systems, Atlas Federal Solutions, Pacific Grid Energy, Keystone Financial Group

## Real clients

**SANEARES** (Brazil): environmental engineering, infrastructure, and sanitation. Official white wordmark from `~/github/saneares/sanedu/frontend/public/logo-saneares-dark.png` (same mark as `.tmp/logo-white.png`). Do not invent a logo. Site: https://saneares.com.br/

## Deploy commands

```bash
cd ~/github/mozentai
bundle exec jekyll build   # local verify
git push origin main       # triggers GitHub Pages
gh run watch $(gh run list --workflow=pages.yml --limit 1 --json databaseId --jq '.[0].databaseId')
```

## Context repo mirror

Canonical rules also live at `~/github/context/mozentai/rules/`. When updating `.cursor/rules/`, copy changes there and run `~/github/context/sync.sh`.

## Learnings log

| Date | Learning |
|------|----------|
| 2026-06-28 | Site must stay English; user saw "Serviços" likely from browser translate |
| 2026-06-28 | Partners should be Axonius-style rolling marquee, not static grid |
| 2026-08-25 | SANEARES is a real client. Use their official logo from `~/github/saneares`, never a made-up mark. |
| 2026-08-25 | Lead with auditing: "Do you know if your company is safe enough?" Keep that copy below the fold. Hero stays wordmark + tagline only. |
| 2026-08-25 | Company tenure is 10 years in the market. Experts are 15+ years only, not a 10+ and 15+ split. CEO is Victor Matos. Use his real LinkedIn photo, never the GitHub astronaut illustration, never a generated face. |
| 2026-08-25 | Do not keep invented marketing stats. Only publish numbers Victor stated. |
| 2026-08-25 | Conway glider (hacker emblem) lives in the footer as a small 56px animation. Do not put it in the hero. Recolor Victor's white/black GIF to mint on `--bg`. Static PNG for reduced motion. |
| 2026-08-25 | CEO card links to Victor's Hack The Box profile spacemany2k38 (https://profile.hackthebox.com/profile/019cab82-c302-71b6-a445-abbfbbb76b89). Show handle and CJCA. Do not copy employer names from the HTB About onto the Mozentai site. |
| 2026-08-25 | NEVER expand CJCA to "Certified Junior Cybersecurity Associate" on the site. Victor is an expert. Show only the abbreviation "HTB CJCA". |
| 2026-08-25 | Public experience claim is 15+ years only. Do not write "10+ and 15+" on the site. Keep company tenure as 10 years in the market. |
| 2026-08-26 | Mozentai is also an outsourcing/staff augmentation company for security engineers. Engineers are hands-on coders with cybersec as their background. Positioning inspired by BairesDev, Inclusion Cloud, Thoughtworks, Avenue Code but niche to cybersecurity. Added Security Engineering service, engagement models section, and updated testimonials to reflect embedded engineers. |
| 2026-08-26 | Client logos must be PNG only. Victor rejected leftover SVG URLs (example: https://mozentai.com/assets/clients/pacific-grid.svg). A stale GitHub Pages deploy of an older commit (8401b1f) overwrote the PNG conversion and put SVGs back live. Do not leave .svg files in assets/clients/. Convert locally, commit PNG, delete SVG. |
| 2026-08-26 | Added /hire/ so people can hire Mozentai. Form fields: name, work email, company, engagement (Staff Augmentation / Dedicated Team / Project-Based / Not sure yet), message. Submissions go to victor@mozentai.com via FormSubmit. Homepage CTA lives below testimonials, not in the hero. First FormSubmit send requires Victor to confirm the activation email. |
| 2026-09-19 | Victor asked to be removed from the public site. Do not render the Leadership section, CEO photo, name, bio, or HTB link on mozentai.com. Keep `_data/leadership.yml` and `assets/team/victor-matos.jpg` off the live page. |
| 2026-09-19 | New offering: CITC, Council of Information Technology & Computing. Credential service for software engineers, CREA/CRM analog for computing. Public copy in English only, no Portuguese council names. Do not invent exam/fee/legal details. Fourth service card, 2x2 grid. Form engagement option: CITC Credential. |
| 2026-09-19 | Public CTA is "Get in touch", never "Hire a security engineer". Footer and /hire/ topbar use the same label. Contact copy does not name Victor. |
| 2026-09-19 | Public positioning is generic toward software engineers, not only cybersecurity. Tagline: Engineers @ Work. Pitch: "Need engineers who actually ship?" Service card "Security Engineering" renamed Engineering. Testimonials dropped attacker/CISO-only framing and the 74% figure. |
| 2026-09-19 | Manager inbox at /login/. Looks like 404. Click wordmark, Google sign-in as spacemany2k38@gmail.com or victor@mozentai.com. IP allowlist lives in Firestore config/allow.ips, not in git. Contact form writes Firestore inquiries plus FormSubmit email. Firebase project mozentai-com. Do not link /login from the public site. Cloud Functions need Blaze, so IP is enforced in the inbox page after sign-in, not at the CDN edge. |
| 2026-09-19 | CITC is the professional standing. Pitch: we prepare the professionals so companies skip screening. Career Wall lookup on the homepage. Engineers hold a SmartHex-16 CITC number (PyPI smarthex 0.2.2). $20/month Stripe subscription keeps the CREA. $200 Stripe one-time exam per title. Max 5 titles per CREA. Public catalog starts with Python Engineer I, II, III. Requirements from Mozentai only. Stripe-Signature is source of standing for engineers and companies. Company plans gate which engineer group they can access. Do not print those prices, the cap, SmartHex, CPF, or CREA on the public page. Portraits on the wall use site colors only. Do not invent registered people. |
| 2026-09-19 | Each CITC title has its own question group and a named exam. Python: P1, P2, P3. Any exam code ending in X is specialist (PX). Two modes: regular questionnaire, and a scheduled sitting with an instructor (the tester). HTB-like in spirit, but candidates book the instructor session. After the sitting, generate a performance type from the test score. Do not invent questions, score bands, or a PX title on the public page. Public wall shows P1/P2/P3 and the two modes only. Question banks live in `_data/exams.yml` and stay unpublished. |
| 2026-09-19 | Hero wordmark must stay large. The old script sized MOZENT△I to the tagline width. After the tagline became "Engineers @ Work", the letters dropped to ~23px. Size the mark to the viewport lockup (cap 560px) and stretch tagline tracking under it. Do not restore the 30px mobile wordmark override. |
| 2026-09-19 | Victor: remove the generic stuff, make it look more professional, leave the client companies. Took Testimonials (invented quotes) and How We Work engagement cards off the public homepage. Clients marquee stays (EU, US, Brazil including SANEARES). Copy tightened: no "who ship", no "Start a conversation", pitch points at the Career Wall. Hire form still lists Staff Augmentation, Dedicated Team, Project-Based, CITC Credential. Keep `_includes/testimonials.html` and `_includes/engagement.html` off the live page, same as Leadership. |
| 2026-09-19 | Victor asked to remove the homepage footer lines "Get in touch" and "victor@mozentai.com". Footer is the Conway glider only. Contact still lives in the Get in touch CTA to `/hire/` and the form still delivers to victor@mozentai.com. Do not put that email or a Get in touch link back in the footer. |
| 2026-09-19 | Added Git (GT1/GT2/GT3/GTX), GitHub (GH1/GH2/GH3/GHX), and GitLab (GL1/GL2/GL3/GLX) certification tracks to the CITC title catalog. Same I/II/III + Specialist pattern. Exam codes: GT for Git, GH for GitHub, GL for GitLab. Each has full curriculum in `_data/exams.yml`. |
| 2026-09-19 | Added engineer login at `/account/` ($20/month CREA keep-alive) and company login at `/company/` ($1,000/month plan). Both use Firebase Auth (Google sign-in) and Stripe Payment Links for subscriptions. Engineers see CITC standing, certifications, freshness. Companies get Career Wall access and engineer lookup. Firestore collections: `engineers/{uid}` and `companies/{uid}`. Stripe webhooks (backend) write subscription status to Firestore. Payment Link URLs in `_data/stripe.yml` need real Stripe URLs. Stripe Customer Portal URL also in that file. |
| 2026-09-19 | CITC titles have two categories: **engineering disciplines** and **tools**. Engineering disciplines use "X Engineer I/II/III" + "X Specialist" naming (Python Engineer I, Java Engineer III, etc.). Tools use plain "X I/II/III/X" naming without "Engineer" (Git I, GitHub II, GitLab X). Tools get a `category: tool` field in `_data/career.yml` and a ◉ seal icon on the Career Wall. AWS/GCP/Azure/Heroku/Firebase are platforms (currently named as "Engineer" but could be reclassified if Victor asks). Git, GitHub, GitLab are tools. |
| 2026-09-19 | About page is about the PROBLEM we solve, not a generic company blurb. Lead: "Technical interviews are broken." Victor likes that headline. Tone must stay professional and measured after it. Do not use "cut the crap", "through and through", "we guarantee quality", "whiteboard riddle", or "never waste another round". Do not revert the lead to "poor signal". Mozentai assesses engineers before they join a team. Career Wall standing is live, certifications have freshness, green means current. 10 years in market, 15+ years experience. CITC exists so companies can look people up and skip the screening round. |
| 2026-09-19 | Account and Company sign-in gates use `.gate { display: flex }`. That author rule overrides the HTML `hidden` attribute, so "Sign in with Google" stayed on screen after Google auth. Pair every `.gate` flex rule with `.gate[hidden] { display: none !important; }`. Same bug on `/login/` inbox. |

## Self-update protocol

- Read this file at session start before editing the site.
- After corrections from the user, append to Learnings log and update relevant rules.


---

## Bitly-standard addendum (conformed 2026-08-11)

Adopted verbatim from the canonical bitly cortex so this project's brain discipline matches the most up-to-date standard. Where these overlap rules above, the stricter wording here wins.

- **Reference library:** A shared, on-disk reference library lives at `~/github/knowledge` (see `knowledge.mdc`), organized by domain (e.g. `hack/`). When Victor asks "do you have a reference?" or needs authoritative backing, search it FIRST and answer in the form "yes, in <source> there's a detail that points out that <exact point>", with the file path. NEVER cite a source that is not actually in the library.
- **Confirmation line requires a REAL write, every turn.** At the end of every exchange, persist to the DB FIRST, then show: `Updated BRAIN <data in the db>`. That line is a CLAIM that a `sqlite3` INSERT/UPDATE actually executed THIS turn. NEVER print it unless the write ran this turn and succeeded. Printing it without the write is fabrication and is forbidden: do not defer, do not "print now and save later". If genuinely nothing changed, run no write and say so plainly.
- **NEVER fabricate data.** Work only with real, provable, testable data. If it is not in `brain.db`, not from a verified tool call, and not explicitly provided by Victor, the answer is "I don't know", so ASK. Every factual claim must trace to a real source.
- **Save MAXIMUM detail, never a few bits.** Every brain write must capture full context: specifics, dates, names, keys, numbers, direct quotes, reasoning, and surrounding circumstances. Verbosity and completeness are correct; brevity is the bug. When in doubt, write MORE.
